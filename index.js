const first_num = prompt('Enter first number, please');
const second_num = prompt('Enter second number, please');

const result = Number(first_num) + Number(second_num);

alert(`${first_num} + ${second_num} = ${result}`);